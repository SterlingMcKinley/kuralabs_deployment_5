version: "3.8"
services:
  nginx:
    build: nginx
    container_name: "nginx-container"
    ports:
      - "5000:5000"
    depends_on:
      - gunicorn-pythonflask
  gunicorn-flask:
    build: gunicorn-pythonflask
    container_name: "gunicorn-pythonflask-container"
